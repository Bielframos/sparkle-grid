import { red, pink, purple, blue, green, teal, orange } from '@radix-ui/colors'
import { fontSizes } from './fontSize'

export const theme = {
  colors: { ...red, ...pink, ...purple, ...blue, ...green, ...teal, ...orange },
  fontSizes: { ...fontSizes },
}

